# FYP
3rd Strike AI Project. Diambra arena framework

## Setup

### Git LFS

This repository uses Git Large File Storage (LFS) for large files. To clone the repository with LFS support, run:

```bash
git lfs install
git clone https://github.com/Brendan-Swords/FYP.git
